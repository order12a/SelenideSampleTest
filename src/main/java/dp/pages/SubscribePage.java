package dp.pages;

import dp.pages.AnyPage;
import org.openqa.selenium.WebDriver;

public class SubscribePage extends AnyPage {
    public SubscribePage(WebDriver driver) {
        super(driver);
    }
}
