package dp.pages.components;

import dp.pages.Page;
import org.openqa.selenium.WebDriver;

public class SearchResultFilterAndOptions extends Page{
    public SearchResultFilterAndOptions(WebDriver driver) {
        super(driver);
    }
}
